# Server
PORT=3000
SESSION_SECRET=ändra-till-en-lång-slumpsträng

# ——— Discord OAuth2 (inloggning) ———
# Skapa en app: https://discord.com/developers/applications → New Application
# Under "OAuth2" → Redirects: lägg till ex. http://localhost:3000/auth/discord/callback
DISCORD_CLIENT_ID=din_client_id
DISCORD_CLIENT_SECRET=din_client_secret
DISCORD_CALLBACK_URL=http://localhost:3000/auth/discord/callback

# ——— FiveM-server (status + anslutningslänk) ———
# IP/hostname och port där din FiveM-server körs (standard 30120)
# Servern måste exponera http://IP:PORT/dynamic.json (vanligt på FiveM)
FIVEM_SERVER_IP=play.example.com
FIVEM_SERVER_PORT=30120

# ——— Valfria länkar ———
# Discord-inbjudan (används i footer på landningssidan och som fallback för dashboard-länkar)
DISCORD_INVITE_URL=https://discord.gg/din-inbjudan

# Länkar för dashboard (Regler, Whitelist, Rapporter, Support). Lämna tomma för att använda DISCORD_INVITE_URL
# RULES_URL=https://discord.com/channels/...
# WHITELIST_URL=https://...
# REPORTS_URL=https://...

# ——— Spelarstatistik (Mina karaktärer, Total speltid) ———
# Om satt måste FiveM-servern skicka samma nyckel i header X-API-Key när den anropar POST /api/player-stats/update
# Lämna tom för att tillåta uppdateringar utan nyckel (endast för utveckling)
# PLAYER_STATS_API_KEY=din-hemliga-nyckel

# ——— MySQL / MariaDB (HeidiSQL) – karaktärer från databas ———
# Om DB_HOST, DB_USER och DB_DATABASE är satta används er databas istället för player-stats.json.
#
# Exempel: ESX Legacy – tabell "users" i databasen esxlegacy_9572da
# Namn från firstname + lastname, jobb från job, pengar från JSON-kolumnen accounts (bank + money),
# speltid från JSON-kolumnen metadata (fältet lastPlaytime).
#
# VIKTIGT: Tabellen users måste ha en kolumn som innehåller spelarens Discord-ID, så portalen
# vet vilka karaktärer som tillhör den inloggade användaren. Om ni inte har det:
# 1) Öppna HeidiSQL → välj databasen → tabell "users"
# 2) Lägg till en ny kolumn t.ex. discord_id (VARCHAR(64), NULL)
# 3) Sätt värdet från er FiveM-server när spelaren är inloggad (t.ex. via ett script som
#    uppdaterar users.discord_id för den karaktär spelaren använder)
#
# DB_HOST=127.0.0.1
# DB_USER=root
# DB_PASSWORD=
# DB_DATABASE=esxlegacy_9572da
# DB_TABLE=users
# DB_COLUMN_DISCORD=discord_id
# Namn från för- och efternamn (ESX)
# DB_COLUMN_FIRSTNAME=firstname
# DB_COLUMN_LASTNAME=lastname
# DB_COLUMN_JOB=job
# Pengar från JSON-kolumnen "accounts" (bank + money)
# DB_COLUMN_ACCOUNTS=accounts
# Speltid från JSON-kolumnen "metadata", fältet lastPlaytime (portalen visar i minuter)
# DB_COLUMN_METADATA=metadata
# DB_PLAYTIME_JSON_KEY=lastPlaytime
# Om ert script sparar speltid i SEKUNDER i stället för minuter, sätt:
# DB_PLAYTIME_UNIT=seconds
#
# Om ni istället har enkla kolumner (namn, jobb, money, playtime_minutes) använd:
# DB_COLUMN_NAME=name
# DB_COLUMN_JOB=job
# DB_COLUMN_MONEY=money
# DB_COLUMN_PLAYTIME=playtime_minutes
